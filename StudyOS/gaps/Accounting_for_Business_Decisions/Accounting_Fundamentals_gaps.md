# Gaps Roll-Up — Accounting_for_Business_Decisions > Accounting_Fundamentals
Last regenerated: 2026-07-17 | After Session 1

> Cross-cluster view of **all remaining** ACTIVE + FRAGILE gaps, **Priority 1 first**. This file is
> **regenerated** each session (full rewrite) — the per-session history lives in `master_status.md`.

## Priority 1 — Confident + Wrong (most dangerous)

- **sources_vs_uses** (01_Recording_Financial_Transactions, node C8) — **Liabilities understood as
  *things the business spent money on* (uses) instead of *claims by outsiders who supplied the money*
  (sources).** Inverts the right-hand side of A = L + E. Under this model, assets and liabilities are
  the same rupees counted twice, and the equation cannot balance.
  *Stated unprompted, confidently, with zero hedging — the exact `spec.md` §4 danger case.*
  — *next: a transaction that re-shapes assets without touching the right side at all (cash →
  machinery). The broken model predicts a change; the correct one predicts neither side moves. Follow
  with a borrowing, where the right side DOES move, so it can't be passed by guessing "nothing
  changes".*

## Priority 2 — Repeatedly ACTIVE (2+ sessions)

*(none — Session 1 is the first session)*

## Priority 3 — Standard ACTIVE

- **asset_vs_expense_boundary** (01_Recording_Financial_Transactions, node C9) — The asset/expense
  line drawn at **"did it produce value"** instead of **"do you still control a resource afterward."**
  Predicts that buying machinery reduces equity. It does not — it's an asset swap.
  *Downstream of C8: the same sources/uses confusion in different clothing.*
  — *next: two payments of identical amount in one question — one buying equipment, one paying rent.
  Which reduced equity, and why? Identical rupees, opposite treatment. The "produced value" model
  cannot separate them.*

## FRAGILE — needs one confident confirmation at a new angle

- **debit_credit_forced_vs_arbitrary** (01_Recording_Financial_Transactions, node C10) — Correct that
  debit/credit are directions, but "convention" applied to the **whole** system. Blurs what is
  arbitrary (naming the left column "debit") with what is forced (which side an account lives on —
  dictated by the equation, not by agreement).
  — *new angle: a novel account absent from the workbook's ten. Require the side to be **derived**
  from A = L + E with reasoning stated. Recall cannot pass this.*

---
Counts: ACTIVE 2 · FRAGILE 1 · UNTOUCHED nodes 78 / 81 · Converged: **no**

**Not close to converged, and the node map is the proof rather than a feeling.** Convergence needs
ACTIVE = 0 **and** FRAGILE ≤ 5 **and** zero UNTOUCHED nodes (`spec.md` §9). 78 of 81 nodes are
untouched because no question set has been issued yet.

**The study sheet covering all 81 nodes does not move this number.** A node leaves UNTOUCHED when a
question on it has been answered — not when it has been explained.

**Note on P1 closure:** the correction for `sources_vs_uses` was delivered in-session on 2026-07-17
and the student accepted it without pushback. **That closes nothing.** Type is `quantitative` —
reading never closes a gap (`spec.md` §2). It requires 5 variants solved with zero hesitation.
