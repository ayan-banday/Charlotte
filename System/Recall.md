---
date created: 2026-06-07
date updated: 2026-06-07
loaded: NEVER auto-loaded — retrieved on demand (grep) by Charlotte or a subagent
purpose: Deep memory. Durable learnings + summaries rolled down from MEMORY.md.
rule: Self-compacts — when long, summarize oldest entries into higher-level summaries
  (summaries of summaries). See /CLAUDE.md.
---

# RECALL

Deep memory. Not loaded at session start — Charlotte greps it (or sends a subagent) only when a
task needs something the hot tier doesn't hold. Keeps main context lean.

**Entry format** (newest on top):

```
### [topic] — YYYY-MM-DD
1–3 lines. The durable learning, not the raw event.
```

---

*(empty — fills as we work)*
